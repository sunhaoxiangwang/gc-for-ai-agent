# reap - build progress

Handoff note for an agent with no memory of previous sessions. Read this first,
then resume from the first unchecked milestone.

## Host log

| Date | Host (uname -s / -m) | Notes |
| --- | --- | --- |
| 2026-09-06 | Darwin / arm64 | Initial session. No Rust toolchain present; installed via rustup (stable, `--no-modify-path`). Use `~/.cargo/bin` on PATH. |

## Milestones

- [ ] **M1. Read-only core.** Config parsing with machine overlays, walker, rule
      matching, sizes and ages, `report`, `explain`, `--json`. No deletion code
      anywhere in the tree.
      *Acceptance: runs clean on macOS and Linux; every rule's matches reviewed by hand.*
- [ ] **M2. Guards and doctor.** Full guard stack, `doctor` incl. TCC probe and
      quarantine rename test. Still no deletion.
      *Acceptance: guard unit tests and escape suite pass; doctor identifies a
      deliberately TCC-blocked root on macOS.*
- [ ] **M3. Executor.** Quarantine rename, run manifest, interrupted-run recovery,
      two-opt-in gating, `sweep --tier 0 --apply`, `gc-session`.
      *Acceptance: executor tests pass; a week of real use shows zero unexpected removals.*
- [ ] **M4. Scheduling and pressure.** `--until-free`, tiers 1 and 2, APFS snapshot
      accounting, systemd units, launchd plist, `install`/`uninstall`.
      *Acceptance: the timer fires and reclaims on both platforms.*
- [ ] **M5. Onboarding.** `init --detect`, `examples/` configs, full README and docs.
      *Acceptance: a stranger goes from clone to first successful `report` using
      only the README.*
- [ ] **M6. Release.** CI matrix, release workflow, install script, crates.io
      publish, CHANGELOG, LICENSE, CONTRIBUTING, SECURITY.
      *Acceptance: a tagged release produces working binaries for all four targets
      and install.sh succeeds on a clean machine.*

## Decisions and deviations

(Recorded as they happen. Where the spec was ambiguous, the more conservative
reading was taken.)

## Deferred

(Nothing yet.)
